const { actionsData } = require('./dist/server.cjs').__esModule ? require('./dist/server.cjs').actionsData : (async () => {
  // Try dynamic import from tsx if needed, but since we built it, we can just compile content.ts to js
})();
